---
phase: 05-make-the-new-path-authoritative
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/authoritative-ci.yml
  - .github/workflows/build_osx.yml
  - .github/workflows/build_osx_asan.yml
  - .github/workflows/build_flatpak.yml
  - .github/workflows/build_flatpak_asan.yml
  - .github/workflows/build_windows.yml
  - tools/ci/setup_authoritative_linux.sh
  - tools/ci/setup_authoritative_macos.sh
  - tools/ci/run_authoritative_ci_linux.sh
  - tools/ci/run_authoritative_ci_macos.sh
  - README.md
  - .github/CONTRIBUTING.md
autonomous: true
requirements:
  - CI-01
  - CI-02
  - CI-03
user_setup: []
must_haves:
  truths:
    - Linux and macOS each have an explicit authoritative CI job that runs the same Bazel-first commands contributors use locally.
    - The repo no longer presents legacy automatic workflows as equal primary gates for build/test/tooling changes.
    - The authoritative CI path handles current dependency bootstrap honestly instead of relying on unstated local-only artifacts.
  artifacts:
    - .github/workflows/authoritative-ci.yml
    - tools/ci/setup_authoritative_linux.sh
    - tools/ci/setup_authoritative_macos.sh
    - tools/ci/run_authoritative_ci_linux.sh
    - tools/ci/run_authoritative_ci_macos.sh
    - .github/CONTRIBUTING.md
  key_links:
    - `authoritative-ci.yml` runs Linux and macOS jobs through the same `./prusa`/Bazel-facing commands used locally.
    - Legacy reusable workflows are demoted to manual or explicitly secondary posture so authority is not ambiguous.
    - Contributor guidance names the authoritative workflow as the build/test/tooling gate.
---

<objective>
Create one authoritative Linux/macOS CI gate for the Bazel-first workflow and demote legacy automatic workflows so the repo has one clear build/test/tooling source of truth.

Purpose: Satisfy the CI authority part of Phase 5 without drifting into packaging or Windows migration.
Output: One authoritative CI workflow, explicit Linux/macOS CI bootstrap/run scripts, and updated workflow posture for legacy non-authoritative jobs.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-make-the-new-path-authoritative/05-CONTEXT.md
@.planning/phases/05-make-the-new-path-authoritative/05-RESEARCH.md
@README.md
@.github/CONTRIBUTING.md
@.github/workflows/build_nogui.yml
@.github/workflows/build_osx.yml
@.github/workflows/build_osx_asan.yml
@.github/workflows/build_flatpak.yml
@.github/workflows/build_flatpak_asan.yml
@.github/workflows/build_windows.yml
@.github/workflows/static_analysis.yml
@prusa
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the authoritative Linux/macOS Bazel CI workflow</name>
  <files>.github/workflows/authoritative-ci.yml, tools/ci/setup_authoritative_linux.sh, tools/ci/setup_authoritative_macos.sh, tools/ci/run_authoritative_ci_linux.sh, tools/ci/run_authoritative_ci_macos.sh</files>
  <action>Create one authoritative workflow that runs on `push` and `pull_request` and has explicit Linux and macOS jobs. Those jobs must exercise the same Bazel-first command surface contributors use locally: product build for the proven slice, bounded tests, formatting check, bounded lint, and compile-command refresh. Use small reusable shell scripts under `tools/ci/` so the workflow stays reviewable and the command surface is not duplicated ad hoc inside YAML. Make dependency bootstrap explicit for each platform, including the current macOS vendor-tree bridge and the current Linux system-library bridge.</action>
  <verify>`sed -n '1,260p' .github/workflows/authoritative-ci.yml && sed -n '1,220p' tools/ci/run_authoritative_ci_linux.sh && sed -n '1,220p' tools/ci/run_authoritative_ci_macos.sh`</verify>
  <done>The repo contains one clear authoritative Linux/macOS workflow that runs the same Bazel-facing commands contributors are expected to use locally.</done>
</task>

<task type="auto">
  <name>Task 2: Demote legacy automatic workflows so the gate is unambiguous</name>
  <files>.github/workflows/build_osx.yml, .github/workflows/build_osx_asan.yml, .github/workflows/build_flatpak.yml, .github/workflows/build_flatpak_asan.yml, .github/workflows/build_windows.yml, README.md, .github/CONTRIBUTING.md</files>
  <action>Adjust the existing legacy reusable-workflow wrappers so out-of-scope packaging/Windows/old-CMake-oriented jobs no longer present themselves as equal automatic gates for build/test/tooling changes. Keep them available where useful, but make their posture explicitly manual or secondary. Update contributor-facing wording so the new authoritative CI workflow is the one maintainers should treat as the source of truth for Linux/macOS build/test/tooling changes.</action>
  <verify>`rg -n \"workflow_dispatch|push:|pull_request:|authoritative|legacy|manual\" .github/workflows/*.yml README.md .github/CONTRIBUTING.md`</verify>
  <done>The workflow inventory and contributor-facing language make the authoritative gate obvious and legacy automation visibly secondary.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `.github/workflows/authoritative-ci.yml` exists and contains explicit Linux and macOS jobs
- [ ] `tools/ci/run_authoritative_ci_linux.sh` and `tools/ci/run_authoritative_ci_macos.sh` call the bounded `./prusa` workflows and proven product build path
- [ ] `rg -n "pull_request:|push:|workflow_dispatch:" .github/workflows/*.yml` shows the authoritative workflow as the automatic Linux/macOS gate and legacy workflows as manual/secondary
- [ ] `rg -n "authoritative|legacy|manual|./prusa" README.md .github/CONTRIBUTING.md` confirms the gate language is explicit
- [ ] No task in this plan migrates packaging or Windows into the authoritative gate
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Linux and macOS have one authoritative Bazel-first CI gate
- Legacy automatic workflows are no longer ambiguous primary gates
- Contributor-facing CI guidance matches the actual workflow posture
</success_criteria>

<output>
After completion, create `.planning/phases/05-make-the-new-path-authoritative/05-01-SUMMARY.md`
</output>
