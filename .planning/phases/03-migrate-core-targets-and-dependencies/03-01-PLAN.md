---
phase: 03-migrate-core-targets-and-dependencies
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MODULE.bazel
  - .bazelrc
  - tools/bazel/platforms/BUILD.bazel
  - tools/bazel/policies/BUILD.bazel
  - tools/bazel/policies/proof_slice_bridges.md
  - tools/bazel/README.md
  - src/BUILD.bazel
  - src/CLI/BUILD.bazel
autonomous: true
requirements:
  - BLD-02
  - DEPS-03
user_setup: []
must_haves:
  truths:
    - macOS can build and run a Bazel-owned `//src:PrusaSlicer` binary label that owns the entry point, compile settings, and link shape even if one temporary handoff still exists behind that label.
    - Any temporary handoff used in this first slice is limited to generated/build products or one named internal seam and is explicitly forbidden from bridging a broad `libslic3r` subtree, `admesh`, or an opaque pack of internal static libraries.
    - The Phase 3 target shape starts from one stable Bazel label graph that later plans deepen instead of replacing with a different graph.
  artifacts:
    - src/BUILD.bazel
    - src/CLI/BUILD.bazel
    - tools/bazel/platforms/BUILD.bazel
    - tools/bazel/policies/proof_slice_bridges.md
    - tools/bazel/README.md
  key_links:
    - `//src:PrusaSlicer` is Bazel-owned at the binary boundary and points at a small CLI-facing library target instead of inheriting a full legacy app target.
    - `tools/bazel/policies/proof_slice_bridges.md` records the initial handoff seam before more source ownership begins.
    - `.bazelrc` and `tools/bazel/platforms/BUILD.bazel` keep macOS as the first proving ground without creating a macOS-only target namespace.
---

<objective>
Establish the smallest real Phase 3 product proof on macOS: a Bazel-owned `PrusaSlicer` binary shell with explicit generated/build-product handling and, if unavoidable, one narrow handoff seam.

Purpose: Reduce the first execution step to a bounded binary-shape proof instead of repeating the failed attempt to prove source ownership, deep internal wiring, dependency migration, and testing all at once.
Output: A stable `//src:PrusaSlicer` label on macOS, an initial bridge inventory, and a written rule that the first bridge may only cross a single narrow seam.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-CONTEXT.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-RESEARCH.md
@.planning/phases/01-evaluate-build-system-fit/01-build-system-decision.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@MODULE.bazel
@.bazelrc
@tools/bazel/README.md
@tools/bazel/platforms/BUILD.bazel
@tools/bazel/policies/system_libraries.bzl
@src/PrusaSlicer.cpp
@src/CMakeLists.txt
@src/CLI/Run.cpp
@src/CLI/Setup.cpp
@version.inc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the smallest Bazel-owned macOS binary shape for `PrusaSlicer`</name>
  <files>src/BUILD.bazel, src/CLI/BUILD.bazel, .bazelrc, tools/bazel/platforms/BUILD.bazel</files>
  <action>Create `//src:PrusaSlicer` as a real Bazel-owned `cc_binary` rooted on `src/PrusaSlicer.cpp` and the smallest CLI-facing target surface needed to launch a non-GUI path on macOS. Keep the public label stable from the start. Do not try to source-own a large `libslic3r` subtree in this plan. If the binary cannot close immediately, stop at a single named handoff seam behind the binary boundary instead of widening the slice.</action>
  <verify>`bazel build --config=macos //src:PrusaSlicer`</verify>
  <done>`//src:PrusaSlicer` exists as a Bazel-owned binary label on macOS and its target graph is intentionally smaller than the previously failed proof slice.</done>
</task>

<task type="auto">
  <name>Task 2: Model direct generated inputs and forbid broad internal binary bridges</name>
  <files>src/BUILD.bazel, src/CLI/BUILD.bazel, tools/bazel/policies/BUILD.bazel, tools/bazel/policies/proof_slice_bridges.md, tools/bazel/README.md</files>
  <action>Bring only directly required generated/build products such as version or config headers into Bazel as explicit rule outputs. If one temporary handoff is still required, define it as a single bridge entry with scope, reason, owner, and retirement condition before using it. Write the rule down explicitly: no bridge may point at a broad prebuilt `libslic3r` artifact, `admesh`, or a bundled set of internal static libraries in this plan.</action>
  <verify>`rg -n "generated|build-product|single seam|not allowed|libslic3r|admesh" tools/bazel/policies/proof_slice_bridges.md tools/bazel/README.md src/BUILD.bazel src/CLI/BUILD.bazel`</verify>
  <done>Generated inputs are explicit Bazel dependencies, and any remaining handoff is documented as one narrow seam instead of an opaque internal binary bridge.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the macOS handoff with a real command path</name>
  <files>.bazelrc, tools/bazel/README.md, src/BUILD.bazel, src/CLI/BUILD.bazel, tools/bazel/policies/proof_slice_bridges.md</files>
  <action>Run the first macOS proof through the repo's Bazel front door and record the exact command shape. Validate that the binary can be built and invoked for a lightweight CLI action such as `--help` or `--version`. If invocation fails because the bridge surface is larger than planned, reduce scope rather than broadening the bridge.</action>
  <verify>`bazel run --config=macos //src:PrusaSlicer -- --help`</verify>
  <done>macOS has a real Bazel-owned binary proof and the first bridge boundary is small, visible, and still reviewable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bazel build --config=macos //src:PrusaSlicer`
- [ ] `bazel run --config=macos //src:PrusaSlicer -- --help`
- [ ] `rg -n "single seam|retire_when|not allowed|libslic3r|admesh" tools/bazel/policies/proof_slice_bridges.md tools/bazel/README.md`
- [ ] No task in this plan source-owns a broad `libslic3r` subtree, migrates GUI targets, or introduces an opaque internal binary pack as a bridge
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- macOS has a real Bazel-owned `PrusaSlicer` binary label with a stable target shape
- The only allowed bridge in this step is narrow enough to describe as one seam or generated/build-product edge
- The failed broad-bridge approach is explicitly ruled out in the artifacts produced by this plan
  </success_criteria>

<output>
After completion, create `.planning/phases/03-migrate-core-targets-and-dependencies/03-01-SUMMARY.md`
</output>
