---
phase: 02-establish-build-graph-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - MODULE.bazel
  - .bazelrc
  - prusa
  - tools/bazel/README.md
  - tools/bazel/platforms/BUILD.bazel
  - tools/bazel/toolchains/BUILD.bazel
  - tools/bazel/policies/BUILD.bazel
  - tools/bazel/policies/system_libraries.bzl
  - doc/Dependencies.md
autonomous: true
requirements:
  - BLD-03
  - DEPS-02
user_setup: []
must_haves:
  truths:
    - Linux and macOS platform branching lives in one obvious Bazel-owned subtree instead of leaking through the repo.
    - System-library exceptions are allowed only through a centralized, reviewable registry with scope and rationale.
    - The root command surface and docs can point to placeholder Bazel platform configs and the exception registry without claiming Phase 3 migration or real toolchain bring-up is done.
  artifacts:
    - tools/bazel/platforms/BUILD.bazel
    - tools/bazel/toolchains/BUILD.bazel
    - tools/bazel/policies/system_libraries.bzl
    - tools/bazel/policies/BUILD.bazel
    - tools/bazel/README.md
    - doc/Dependencies.md
    - MODULE.bazel
    - .bazelrc
    - prusa
  key_links:
    - `MODULE.bazel` wires placeholder platform/toolchain registration hooks for the definitions under `tools/bazel/` without enabling real target-level toolchain bring-up yet.
    - `.bazelrc` and `prusa` expose the centralized Linux/macOS config names instead of scattering platform choices through ad hoc flags.
    - `doc/Dependencies.md` points to `tools/bazel/policies/system_libraries.bzl` as the only approval registry for system-library exceptions.
---

<objective>
Define the centralized Bazel platform/toolchain layout and the system-library exception policy structure that Phase 3 will build on.

Purpose: Satisfy Phase 2's policy and graph-structure goals without drifting into full dependency or product-target migration.
Output: A dedicated Bazel subtree for Linux/macOS platform organization, placeholder module registration for those definitions, and one explicit system-library exception registry.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/02-establish-build-graph-skeleton/02-CONTEXT.md
@.planning/phases/02-establish-build-graph-skeleton/02-RESEARCH.md
@.planning/phases/01-evaluate-build-system-fit/01-build-system-decision.md
@doc/Dependencies.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the centralized Linux/macOS Bazel platform and toolchain packages</name>
  <files>MODULE.bazel, .bazelrc, tools/bazel/README.md, tools/bazel/platforms/BUILD.bazel, tools/bazel/toolchains/BUILD.bazel</files>
  <action>Create a Bazel-specific subtree under `tools/bazel/` that centralizes Linux and macOS platform definitions, future extension points, and placeholder toolchain-registration structure. Use Bazel built-in OS constraints first, reserve Windows for an obvious placeholder only, and wire only skeletal registration hooks from `MODULE.bazel` rather than implementing or enabling real target-level toolchain bring-up. Update `.bazelrc` and the Bazel docs so the repo-root command surface can select these configs by name. Keep this work structural: do not start porting broad `src/` targets, do not spread `select()` logic into product packages, and do not treat Phase 2 as the place to implement full toolchain definitions.</action>
  <verify>`bazel query //tools/bazel/platforms:all //tools/bazel/toolchains:all && rg -n "@platforms//os:(linux|osx)|register_(toolchains|execution_platforms)|placeholder|skeleton|Phase 3" MODULE.bazel .bazelrc tools/bazel/README.md tools/bazel/platforms/BUILD.bazel tools/bazel/toolchains/BUILD.bazel`</verify>
  <done>The repo has one obvious Bazel-owned location for Linux/macOS platform branching and placeholder toolchain-registration structure, and the root command surface can point to it by named config without implying full toolchain implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Define the centralized system-library exception registry and policy</name>
  <files>tools/bazel/policies/BUILD.bazel, tools/bazel/policies/system_libraries.bzl, doc/Dependencies.md</files>
  <action>Create one Bazel policy registry for system-library exceptions and move the repo's Phase 2 policy language there. The registry schema must capture, at minimum, the library name, allowed platforms, scope, rationale, and whether the exception is temporary or long-term. Use `doc/Dependencies.md` to distinguish the current legacy dependency inventory from Bazel-approved exceptions: do not silently treat every historically dynamic Linux dependency as approved for the Bazel path. If the skeleton does not yet need a real exception, keep the registry empty but real, documented, and ready for Phase 3 additions.</action>
  <verify>`rg -n "source-fetched|exception|platform|scope|rationale|temporary|long-term|legacy inventory" tools/bazel/policies/system_libraries.bzl doc/Dependencies.md`</verify>
  <done>Maintainers can point to one file that defines how system-library exceptions are approved and one document that explains the difference between existing legacy dependencies and Bazel-approved exceptions.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the platform and policy structure back into the front door</name>
  <files>prusa, .bazelrc, tools/bazel/README.md, doc/Dependencies.md</files>
  <action>Update the wrapper help and Bazel docs so contributors can discover the centralized platform configs and maintainers can discover the exception registry from the same front door introduced in Plan 02-01. The wrapper should expose the Linux/macOS config names or flags that map to the new platform packages, and the docs should show the raw Bazel equivalents. Keep the messaging explicit that these are skeleton controls for later migration work, not proof that the full PrusaSlicer application graph already builds under Bazel.</action>
  <verify>`./prusa help && rg -n "linux|macos|system library|system_libraries\\.bzl|bazel " prusa tools/bazel/README.md doc/Dependencies.md`</verify>
  <done>The repo-root command surface, Bazel subtree docs, and dependency policy docs all point to the same centralized platform and exception structure without introducing migration drift.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bazel query //tools/bazel/platforms:all //tools/bazel/toolchains:all //tools/bazel/policies:all` succeeds for the placeholder structural packages introduced in this phase
- [ ] `./prusa help` shows the named Linux/macOS Bazel path and points users at the direct Bazel commands
- [ ] `rg -n "scope|rationale|temporary|long-term|source-fetched" tools/bazel/policies/system_libraries.bzl doc/Dependencies.md` confirms the exception policy is explicit
- [ ] No task in this plan performs full dependency migration, packaging work, CI completion, or broad product-target bring-up
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Linux and macOS platform/toolchain placeholder structure is centralized under a Bazel-owned subtree
- System-library exceptions have one explicit registry with reviewable metadata instead of scattered approvals
- The repo-root Bazel front door now points to structural platform configs and policy files without overstating migration progress or implying real toolchain bring-up
  </success_criteria>

<output>
After completion, create `.planning/phases/02-establish-build-graph-skeleton/02-02-SUMMARY.md`
</output>
