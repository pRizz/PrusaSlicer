---
phase: 06-backfill-verification-evidence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-migrate-core-targets-and-dependencies/03-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - BLD-01
  - BLD-02
  - DEPS-01
  - DEPS-03
user_setup: []
must_haves:
  truths:
    - Maintainers can point to one phase-level verification report that proves the completed Phase 3 Linux and macOS authoritative build outcomes against the current repo state.
    - Maintainers can point to one phase-level verification report that proves the completed Phase 3 dependency-ownership and bridge-tracking outcomes against the current repo state.
    - Phase-level traceability and milestone audit metadata no longer describe BLD-01, BLD-02, DEPS-01, and DEPS-03 as partial solely because Phase 3 lacked `03-VERIFICATION.md`.
  artifacts:
    - .planning/phases/03-migrate-core-targets-and-dependencies/03-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/v1.0-MILESTONE-AUDIT.md
  key_links:
    - `03-VERIFICATION.md` consolidates the already-landed Phase 3 summaries plus live repo evidence into one auditable report instead of reopening implementation scope.
    - `REQUIREMENTS.md`, `ROADMAP.md`, and `STATE.md` reflect that the evidence gap is closed and that the four affected requirements are now fully verified.
    - `v1.0-MILESTONE-AUDIT.md` is refreshed from the new verification artifact so the milestone can treat the affected requirements as satisfied rather than partial.
---

<objective>
Close the milestone audit blocker by backfilling the missing Phase 3 verification evidence from the live repo state and reconciling the planning metadata that still marks the affected requirements as partial.

Purpose: Finish the v1.0 gap-closure work without reopening Phase 3 implementation or introducing new build/tooling scope.
Output: One Phase 3 verification report plus the smallest traceability and audit metadata refresh needed to show BLD-01, BLD-02, DEPS-01, and DEPS-03 as fully verified.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/06-backfill-verification-evidence/06-RESEARCH.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-01-SUMMARY.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-02-SUMMARY.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-03-SUMMARY.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-04-SUMMARY.md
@.planning/phases/04-add-local-tooling-and-validation/04-VERIFICATION.md
@.planning/phases/05-make-the-new-path-authoritative/05-VERIFICATION.md
@src/BUILD.bazel
@tests/libslic3r/BUILD.bazel
@tools/bazel/deps/proof_slice_deps.bzl
@tools/bazel/policies/proof_slice_bridges.md
@tools/bazel/policies/system_libraries.bzl
@tools/bazel/README.md
@prusa
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-check the live Phase 3 proof against the current repo state</name>
  <files>.planning/phases/03-migrate-core-targets-and-dependencies/03-01-SUMMARY.md, .planning/phases/03-migrate-core-targets-and-dependencies/03-02-SUMMARY.md, .planning/phases/03-migrate-core-targets-and-dependencies/03-03-SUMMARY.md, .planning/phases/03-migrate-core-targets-and-dependencies/03-04-SUMMARY.md, src/BUILD.bazel, tests/libslic3r/BUILD.bazel, tools/bazel/deps/proof_slice_deps.bzl, tools/bazel/policies/proof_slice_bridges.md, tools/bazel/policies/system_libraries.bzl, tools/bazel/README.md, prusa</files>
  <action>Compare the completed Phase 3 summaries with the current build labels, dependency metadata, bridge inventory, system-library exception registry, and documented command surface. Confirm that the same bounded labels still exist for macOS and Linux, that explicit proof-slice dependency ownership is still recorded in version-pinned metadata, and that temporary bridge boundaries still have ownership and retirement criteria. If the live evidence no longer matches the Phase 3 claims, stop at the smallest corrective alignment needed to make the verification story honest; do not reopen broader implementation work.</action>
  <verify>`npx -y @bazel/bazelisk query //src:PrusaSlicer //tests/libslic3r:config_test && rg -n "proof_slice|owner|retire_when|status|temporary|linux|macos|PrusaSlicer|config_test" tools/bazel/deps/proof_slice_deps.bzl tools/bazel/policies/proof_slice_bridges.md tools/bazel/policies/system_libraries.bzl tools/bazel/README.md prusa`</verify>
  <done>The current repo state still substantiates the four Phase 3 requirement claims, and any remaining bounds are explicitly classified as tracked exceptions rather than fresh implementation gaps.</done>
</task>

<task type="auto">
  <name>Task 2: Write the missing Phase 3 verification report from live evidence</name>
  <files>.planning/phases/03-migrate-core-targets-and-dependencies/03-VERIFICATION.md</files>
  <action>Create `03-VERIFICATION.md` in the Phase 3 directory using the established verification-report shape from later phases. The report must cover the Phase 3 goal, the exact verification date, the Linux and macOS build/test outcomes for `//src:PrusaSlicer` and `//tests/libslic3r:config_test`, the explicit dependency-ownership evidence in `proof_slice_deps.bzl`, and the bridge/system-library evidence in `proof_slice_bridges.md` and `system_libraries.bzl`. Keep the report honest about bounded scope and remaining temporary bridges, but frame them as explicit non-blocking boundaries already accepted by the milestone.</action>
  <verify>`test -f .planning/phases/03-migrate-core-targets-and-dependencies/03-VERIFICATION.md && rg -n "BLD-01|BLD-02|DEPS-01|DEPS-03|PrusaSlicer|config_test|proof_slice_deps|proof_slice_bridges|system_libraries" .planning/phases/03-migrate-core-targets-and-dependencies/03-VERIFICATION.md`</verify>
  <done>Phase 3 has one auditable verification artifact that consolidates build, dependency, bridge, and exception evidence from the live repo into a milestone-usable report.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the planning metadata and audit status with the new verification artifact</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <action>Refresh only the planning metadata needed to close the audit evidence gap. Mark BLD-01, BLD-02, DEPS-01, and DEPS-03 complete in `REQUIREMENTS.md`; mark Phase 6 and plan 06-01 complete in `ROADMAP.md`; update `STATE.md` so the blocker is closed and the current focus moves past Phase 6; and regenerate or rewrite the milestone audit so the missing-verification finding disappears and the four requirements are no longer partial. Keep the edits tightly scoped to evidence closure and traceability truthfulness.</action>
  <verify>`rg -n "BLD-01|BLD-02|DEPS-01|DEPS-03|Phase 6|06-01|partial|missing|03-VERIFICATION" .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/v1.0-MILESTONE-AUDIT.md`</verify>
  <done>The planning metadata and milestone audit now agree that Phase 3 is fully verified and that Phase 6 closed a process/evidence gap rather than shipping new implementation scope.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx -y @bazel/bazelisk build --config=dev --config=macos //src:PrusaSlicer`
- [ ] `npx -y @bazel/bazelisk test --config=dev --config=macos //tests/libslic3r:config_test`
- [ ] `npx -y @bazel/bazelisk run --config=dev --config=macos //src:PrusaSlicer -- --help`
- [ ] `docker run --rm --platform=linux/arm64 --user 0:0 -v "$PWD:/workspace" -w /workspace ubuntu:24.04 bash -lc 'set -euo pipefail; export DEBIAN_FRONTEND=noninteractive; apt-get update >/dev/null; apt-get install -y ca-certificates curl git unzip zip openjdk-21-jdk build-essential python3 pkg-config cmake ninja-build libboost-all-dev libtbb-dev libexpat1-dev libpng-dev catch2 >/dev/null; curl -fsSL -o /usr/local/bin/bazelisk https://github.com/bazelbuild/bazelisk/releases/download/v1.22.0/bazelisk-linux-arm64; chmod +x /usr/local/bin/bazelisk; mkdir -p /tmp/codex-home /tmp/bazelroot; HOME=/tmp/codex-home bazelisk --output_user_root=/tmp/bazelroot build --config=dev --config=linux //src:PrusaSlicer; HOME=/tmp/codex-home bazelisk --output_user_root=/tmp/bazelroot test --config=dev --config=linux //tests/libslic3r:config_test'`
- [ ] `npx -y @bazel/bazelisk query 'deps(//src:PrusaSlicer)'`
- [ ] `npx -y @bazel/bazelisk query 'deps(//tests/libslic3r:config_test)'`
- [ ] `rg -n "owner|retire_when|status|temporary|lifetime|allowed_platforms|proof_slice|version" tools/bazel/deps/proof_slice_deps.bzl tools/bazel/policies/proof_slice_bridges.md tools/bazel/policies/system_libraries.bzl tools/bazel/README.md`
- [ ] `.planning/phases/03-migrate-core-targets-and-dependencies/03-VERIFICATION.md` exists and explicitly covers BLD-01, BLD-02, DEPS-01, and DEPS-03
- [ ] `.planning/v1.0-MILESTONE-AUDIT.md` no longer marks those four requirements as partial due only to missing Phase 3 verification
- [ ] No task in this plan expands into new Bazel target migration, new tooling surfaces, or unrelated repo cleanup
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Phase 3 has a phase-level verification report that is grounded in live repo evidence
- BLD-01, BLD-02, DEPS-01, and DEPS-03 are fully verified rather than process-blocked by a missing artifact
- The milestone audit and planning metadata agree that Phase 6 closed the evidence gap without reopening implementation scope
</success_criteria>

<output>
After completion, create `.planning/phases/06-backfill-verification-evidence/06-01-SUMMARY.md`
</output>
