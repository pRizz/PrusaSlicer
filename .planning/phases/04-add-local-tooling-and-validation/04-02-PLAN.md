---
phase: 04-add-local-tooling-and-validation
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - prusa
  - .bazelrc
  - tools/bazel/BUILD.bazel
  - tools/bazel/README.md
  - tools/bazel/format/BUILD.bazel
  - tools/bazel/lint/BUILD.bazel
  - tools/bazel/compdb/BUILD.bazel
  - tools/bazel/format/clang_format_targets.bzl
  - tools/bazel/lint/clang_tidy_targets.bzl
  - tools/bazel/compdb/refresh_compile_commands.sh
  - .clang-tidy
  - .clangd
  - README.md
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03
user_setup: []
must_haves:
  truths:
    - Contributors can run one obvious formatting workflow through the authoritative path with distinct check and fix entry points and explicit exclusions.
    - Contributors can run one bounded, high-signal lint/static-analysis pass through the authoritative path, and that pass is scoped narrowly enough that failures are actionable.
    - Contributors can generate or refresh editor metadata for the authoritative Bazel slice from one obvious command and point `clangd` at the scratch-safe generated path `build/compdb/compile_commands.json` without invoking the legacy build.
  artifacts:
    - prusa
    - .bazelrc
    - tools/bazel/BUILD.bazel
    - tools/bazel/format/BUILD.bazel
    - tools/bazel/lint/BUILD.bazel
    - tools/bazel/compdb/BUILD.bazel
    - .clang-tidy
    - .clangd
    - README.md
    - tools/bazel/README.md
  key_links:
    - `prusa` exposes `fmt --check`, `fmt --fix`, `lint`, and `compdb` as thin wrappers around concrete Bazel-owned tooling targets.
    - The formatting and lint targets operate on an explicitly bounded C/C++ surface tied to the migrated slice and documented exclusions instead of whole-repo pseudo-coverage.
    - `compdb` refresh writes to `build/compdb/compile_commands.json`, which is scratch-safe because the repo already ignores `/build/`, and `.clangd` plus contributor docs reference that exact path directly.
---

<objective>
Replace the placeholder local-tooling commands with real bounded formatting, lint, and editor-metadata workflows that run through the authoritative Bazel path.

Purpose: Complete the Phase 4 contributor tooling surface without drifting into whole-repo enforcement, CI gating, or magical always-fresh editor integration.
Output: Real Bazel-backed `fmt`, `lint`, and `compdb` targets, root wrapper/docs updates, and explicit repo metadata/config files that make scope, refresh expectations, and the scratch-safe metadata output path clear.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/skills/gsd-execute-phase/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-add-local-tooling-and-validation/04-CONTEXT.md
@.planning/phases/04-add-local-tooling-and-validation/04-RESEARCH.md
@.planning/phases/04-add-local-tooling-and-validation/04-01-SUMMARY.md
@prusa
@.bazelrc
@tools/bazel/BUILD.bazel
@tools/bazel/README.md
@README.md
@.clang-format
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace placeholder formatting with explicit check and fix targets</name>
  <files>prusa, .bazelrc, tools/bazel/BUILD.bazel, tools/bazel/format/BUILD.bazel, tools/bazel/format/clang_format_targets.bzl, tools/bazel/README.md, README.md</files>
  <action>Implement real Bazel-backed formatting targets for the main C/C++ contributor surface using the repo's `.clang-format`. Expose separate check and fix entry points, wire them through `./prusa fmt --check` and `./prusa fmt --fix`, and make the bounded file scope explicit in the Bazel definitions or supporting macro. Choose the main contributor-owned C/C++ paths first and record any excluded directories, generated files, or deferred legacy surfaces visibly instead of silently pretending they are formatted.</action>
  <verify>`./prusa fmt --check --dry-run && ./prusa fmt --fix --dry-run && bazel query //tools/bazel/format:all`</verify>
  <done>Formatting is no longer a placeholder: contributors have one obvious authoritative check/fix workflow with explicit scope and exclusions.</done>
</task>

<task type="auto">
  <name>Task 2: Add a bounded high-signal lint path tied to the migrated slice</name>
  <files>prusa, .bazelrc, tools/bazel/BUILD.bazel, tools/bazel/lint/BUILD.bazel, tools/bazel/lint/clang_tidy_targets.bzl, .clang-tidy, tools/bazel/README.md, README.md</files>
  <action>Create a first-pass lint/static-analysis command using `clang-tidy` or the simplest equivalent that can operate on real Bazel-generated metadata. Scope it to the migrated proof slice and, at most, one small adjacent contributor surface already proven by Plan 04-01. Add a repo-root `.clang-tidy` that captures the intended baseline and explicit deferrals so the command stays actionable. Do not expand lint to the whole tree and do not let noisy legacy warnings swamp the output.</action>
  <verify>`./prusa lint --dry-run && bazel query //tools/bazel/lint:all && test -f .clang-tidy`</verify>
  <done>Contributors can run one bounded lint command from the authoritative path, and its policy/configuration makes the intentional scope and deferred warning debt visible.</done>
</task>

<task type="auto">
  <name>Task 3: Land one explicit editor-metadata refresh path and root editor hints</name>
  <files>prusa, .bazelrc, tools/bazel/BUILD.bazel, tools/bazel/compdb/BUILD.bazel, tools/bazel/compdb/refresh_compile_commands.sh, .clangd, tools/bazel/README.md, README.md</files>
  <action>Implement a concrete `./prusa compdb` path that generates or refreshes Bazel-backed editor metadata for the migrated slice at the explicit scratch-safe path `build/compdb/compile_commands.json`, reusing the repo's already ignored `/build/` tree instead of writing an unignored root artifact. Add the smallest root editor hint file needed, such as `.clangd`, only if it points contributors and editors at that exact generated path without hiding the need to refresh it. Update the wrapper help and docs so they name `build/compdb/compile_commands.json` directly and document when contributors should rerun the command.</action>
  <verify>`./prusa compdb --dry-run && bazel query //tools/bazel/compdb:all && rg -n "build/compdb/compile_commands\\.json|compdb|refresh" .clangd README.md tools/bazel/README.md prusa`</verify>
  <done>The repo has one obvious authoritative metadata refresh command, one explicit scratch-safe output contract at `build/compdb/compile_commands.json`, and one matching `.clangd` plus docs story for using that path without legacy build generation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `./prusa fmt --check`
- [ ] `./prusa fmt --fix`
- [ ] `./prusa lint`
- [ ] `./prusa compdb`
- [ ] `test -f .clang-tidy && test -f .clangd`
- [ ] `rg -n "build/compdb/compile_commands\\.json|exclude|defer|bounded|clang-format|clang-tidy" README.md tools/bazel/README.md .clang-tidy .clangd prusa`
- [ ] `bazel query //tools/bazel/format:all //tools/bazel/lint:all //tools/bazel/compdb:all` succeeds
- [ ] No task in this plan expands into whole-repo enforcement, CI gating, or legacy-build-generated editor metadata
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Formatting, lint, and editor metadata all run through one obvious authoritative local workflow
- Each workflow has an explicit bounded scope and visible temporary exclusions or deferrals
- `clangd` or equivalent editor support works from Bazel-generated metadata at `build/compdb/compile_commands.json` without invoking the legacy build
  </success_criteria>

<output>
After completion, create `.planning/phases/04-add-local-tooling-and-validation/04-02-SUMMARY.md`
</output>
