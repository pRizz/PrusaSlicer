module(
    name = "prusaslicer_phase1_eval",
    version = "0.1.0",
)

bazel_dep(name = "rules_cc", version = "0.2.17")
bazel_dep(name = "hedron_compile_commands", dev_dependency = True)

git_override(
    module_name = "hedron_compile_commands",
    remote = "https://github.com/hedronvision/bazel-compile-commands-extractor.git",
    commit = "0e990032f3c5a866e72615cf67e5ce22186dcb97",
)

# Phase 1 note:
# This is a narrow comparison prototype, not the future repo-root module.
# External dependencies are intentionally not modeled fully here; the goal is
# to expose proof-slice and bridge pressure, not to complete migration work.
