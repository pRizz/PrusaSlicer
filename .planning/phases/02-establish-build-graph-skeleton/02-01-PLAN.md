---
phase: 02-establish-build-graph-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MODULE.bazel
  - .bazelrc
  - prusa
  - tools/bazel/BUILD.bazel
  - README.md
  - doc/How to build - Linux et al.md
  - doc/How to build - Mac OS.md
autonomous: true
requirements:
  - BLD-03
user_setup: []
must_haves:
  truths:
    - The repository root exposes Bazel as the obvious first-class build entry point.
    - Contributors can discover one task-oriented command surface at `./prusa` for `build`, `test`, `fmt`, `lint`, `compdb`, and `help`, and each command shows its direct Bazel equivalent.
    - The root skeleton is explicit about being Phase 2 structure only and does not pretend the full PrusaSlicer target graph is already migrated.
  artifacts:
    - MODULE.bazel
    - .bazelrc
    - prusa
    - tools/bazel/BUILD.bazel
    - README.md
    - doc/How to build - Linux et al.md
    - doc/How to build - Mac OS.md
  key_links:
    - `prusa` delegates to Bazel configurations defined in `.bazelrc` instead of embedding a second build DSL.
    - The root docs show `./prusa` first and the raw `bazel` command second for the same workflow.
    - The Bazel helper package under `tools/bazel/` provides the command-surface landing zone without dragging Phase 3 product migration into Phase 2.
---

<objective>
Create the Bazel-first repository root skeleton: root module/config files, a thin `./prusa` command surface, and contributor docs that make the new front door obvious.

Purpose: Satisfy Phase 2's "one authoritative command surface" goal before any broad target migration starts.
Output: Root Bazel entry artifacts, a repo-root wrapper, and Bazel-first build guidance for Linux and macOS contributors.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/02-establish-build-graph-skeleton/02-CONTEXT.md
@.planning/phases/02-establish-build-graph-skeleton/02-RESEARCH.md
@.planning/phases/01-evaluate-build-system-fit/01-build-system-decision.md
@README.md
@doc/How to build - Linux et al.md
@doc/How to build - Mac OS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the repo-root Bazel boundary and default configs</name>
  <files>MODULE.bazel, .bazelrc, tools/bazel/BUILD.bazel</files>
  <action>Create the root Bazel-first artifacts needed for Phase 2 without migrating real product targets yet. Add `MODULE.bazel` as the authoritative repo-root module boundary, create a thin `.bazelrc` with shared configs such as `dev`, `linux`, `macos`, and `compdb`, and add a small `tools/bazel/BUILD.bazel` package that serves as the landing zone for command-surface helper labels or placeholder tooling targets. Keep the root package empty unless Bazel itself forces a minimal root `BUILD.bazel`; if that edge appears, keep it tiny and explain why in the summary instead of turning the repo root into a catch-all package.</action>
  <verify>`test -f MODULE.bazel && test -f .bazelrc && rg -n "common:dev|build:linux|build:macos|build:compdb|name = \\\"(fmt|lint|compdb)\\\"" .bazelrc tools/bazel/BUILD.bazel`</verify>
  <done>The repo has an obvious Bazel root boundary, shared config names exist for the wrapper to consume, and the helper-package landing zone exists without pretending the application graph is already ported.</done>
</task>

<task type="auto">
  <name>Task 2: Add the thin `./prusa` front door with direct Bazel escape hatches</name>
  <files>prusa, .bazelrc, tools/bazel/BUILD.bazel</files>
  <action>Implement a repo-root `./prusa` launcher with task-oriented subcommands `build`, `test`, `fmt`, `lint`, `compdb`, and `help`. The wrapper must stay thin: it should select repo defaults from `.bazelrc`, print or expose the exact underlying Bazel invocation for every supported subcommand, and support a dry-run or print-command mode so contributors can learn the real Bazel command path immediately. Do not encode product-target migration logic, dependency discovery, or platform-specific branching inside shell conditionals beyond choosing the appropriate named Bazel config.</action>
  <verify>`./prusa help && ./prusa build --dry-run && ./prusa test --dry-run && ./prusa fmt --dry-run && ./prusa lint --dry-run && ./prusa compdb --dry-run`</verify>
  <done>Contributors can start from one obvious root command, all supported actions (`build`, `test`, `fmt`, `lint`, `compdb`, and `help`) reveal their direct Bazel equivalents, and the wrapper remains a thin convenience layer rather than a second build system.</done>
</task>

<task type="auto">
  <name>Task 3: Re-anchor root and platform docs on the Bazel-first path</name>
  <files>README.md, doc/How to build - Linux et al.md, doc/How to build - Mac OS.md</files>
  <action>Update the contributor-facing docs so the repository root clearly communicates the new authoritative path. Add a short Bazel-first section to `README.md` that shows `./prusa` first and the direct Bazel equivalent second, and revise the Linux/macOS build docs to demote the CMake flow to legacy or transition status instead of presenting it as a competing primary path. Be explicit that Phase 2 establishes the root skeleton and command surface only; full PrusaSlicer target migration belongs to Phase 3.</action>
  <verify>`rg -n "\\./prusa (build|test|fmt|lint|compdb)|bazel (build|test|run)|legacy|transition" README.md "doc/How to build - Linux et al.md" "doc/How to build - Mac OS.md"`</verify>
  <done>A contributor opening the repo root or platform build docs can immediately tell that Bazel is the new front door, how to invoke it through `./prusa`, and that the old CMake path is no longer the authoritative workflow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f MODULE.bazel && test -f .bazelrc && test -x ./prusa`
- [ ] `./prusa help` lists `build`, `test`, `fmt`, `lint`, `compdb`, and `help`, and the dry-run output for each executable subcommand shows the direct Bazel equivalent
- [ ] `rg -n "\\./prusa|bazel " README.md "doc/How to build - Linux et al.md" "doc/How to build - Mac OS.md"` confirms the Bazel-first docs are present
- [ ] No task in this plan attempts full dependency migration, packaging work, CI wiring, or broad product-target bring-up
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- The repo root now has one obvious Bazel-first entry path for contributors
- The wrapper and docs expose Bazel directly instead of hiding it behind custom build logic
- Phase 3 target migration remains clearly deferred rather than implied complete
  </success_criteria>

<output>
After completion, create `.planning/phases/02-establish-build-graph-skeleton/02-01-SUMMARY.md`
</output>
