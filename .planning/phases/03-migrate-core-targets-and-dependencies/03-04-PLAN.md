---
phase: 03-migrate-core-targets-and-dependencies
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - .bazelrc
  - tools/bazel/platforms/BUILD.bazel
  - tools/bazel/policies/BUILD.bazel
  - tools/bazel/policies/system_libraries.bzl
  - tools/bazel/policies/proof_slice_bridges.md
  - tools/bazel/README.md
  - src/BUILD.bazel
  - src/CLI/BUILD.bazel
  - src/libslic3r/BUILD.bazel
  - tests/libslic3r/BUILD.bazel
autonomous: true
requirements:
  - BLD-01
  - BLD-02
  - DEPS-01
  - DEPS-03
user_setup: []
must_haves:
  truths:
    - Linux builds and tests the same `//src:PrusaSlicer` and `//tests/libslic3r:config_test` labels already proven on macOS.
    - Platform differences are expressed through `select()`, constraints, or narrow compatibility guards rather than duplicated target trees.
    - Every remaining proof-slice exception is classified as owned now, temporary bridge, or explicit Phase 3 deferral with retirement criteria.
  artifacts:
    - tools/bazel/platforms/BUILD.bazel
    - tools/bazel/policies/system_libraries.bzl
    - tools/bazel/policies/proof_slice_bridges.md
    - tools/bazel/README.md
  key_links:
    - The same binary and `config_test` labels run on both macOS and Linux.
    - `tools/bazel/policies/system_libraries.bzl` and `tools/bazel/policies/proof_slice_bridges.md` describe the same remaining exceptions instead of splitting policy across files.
    - `tools/bazel/README.md` shows the exact macOS and Linux proof commands for the bounded slice and where maintainers inspect bridge status.
---

<objective>
Reproduce the bounded Phase 3 slice on Linux and close the phase with explicit cross-platform evidence, ownership boundaries, and bridge retirement rules.

Purpose: Finish the CLI/core migration proof without reopening scope into GUI, packaging, CI, or broad dependency ownership.
Output: Linux evidence for `//src:PrusaSlicer` and `//tests/libslic3r:config_test`, explicit platform branching, and a final bridge/dependency classification for the Phase 3 slice.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-CONTEXT.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-RESEARCH.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-03-SUMMARY.md
@.planning/codebase/STACK.md
@.planning/codebase/CONCERNS.md
@.bazelrc
@tools/bazel/platforms/BUILD.bazel
@tools/bazel/policies/system_libraries.bzl
@tools/bazel/policies/proof_slice_bridges.md
@tools/bazel/README.md
@src/CLI/Setup.cpp
@src/libslic3r/BUILD.bazel
@tests/libslic3r/BUILD.bazel
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bring `//src:PrusaSlicer` and `//tests/libslic3r:config_test` up on Linux</name>
  <files>.bazelrc, tools/bazel/platforms/BUILD.bazel, src/BUILD.bazel, src/CLI/BUILD.bazel, src/libslic3r/BUILD.bazel, tests/libslic3r/BUILD.bazel</files>
  <action>Extend the already bounded macOS slice to Linux using the same `//src:PrusaSlicer` and `//tests/libslic3r:config_test` labels. Use `select()`, platform constraints, or narrow compatibility guards for genuine OS differences such as system libs or startup glue. Do not duplicate the target graph into Linux-only labels and do not let Linux blockers justify widening the slice.</action>
  <verify>`bazel build --config=linux //src:PrusaSlicer && bazel test --config=linux //tests/libslic3r:config_test`</verify>
  <done>Linux proves the same bounded target shape that macOS already established.</done>
</task>

<task type="auto">
  <name>Task 2: Classify every remaining blocker as owned, bridged, or deferred</name>
  <files>tools/bazel/policies/system_libraries.bzl, tools/bazel/policies/proof_slice_bridges.md, src/libslic3r/BUILD.bazel, tests/libslic3r/BUILD.bazel</files>
  <action>Audit all Linux-specific blockers and force each one into exactly one bucket: own it now in the bounded slice, keep it as a narrow temporary bridge with retirement criteria, or defer it explicitly because it belongs to GUI, packaging, or a broader dependency phase. Keep system-library exceptions synchronized with the bridge inventory so no platform exception hides only in build logic.</action>
  <verify>`rg -n "retire_when|status|owner|Linux|macOS" tools/bazel/policies/proof_slice_bridges.md tools/bazel/policies/system_libraries.bzl`</verify>
  <done>No remaining proof-slice blocker is implicit; each is owned, bridged, or deferred with a visible rationale.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the bounded cross-platform proof commands and revalidate macOS</name>
  <files>tools/bazel/README.md, .bazelrc, tools/bazel/policies/proof_slice_bridges.md</files>
  <action>Document the exact commands for building and testing the bounded Phase 3 slice on macOS and Linux and point maintainers at the bridge inventory. Re-run the macOS binary and representative test after Linux adjustments so the phase closes with one shared target shape, not one platform fixed at the expense of the other.</action>
  <verify>`bazel build --config=macos //src:PrusaSlicer && bazel test --config=macos //tests/libslic3r:config_test`</verify>
  <done>The repo records one shared proof shape with explicit commands and both supported platforms validated against it.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bazel build --config=linux //src:PrusaSlicer`
- [ ] `bazel test --config=linux //tests/libslic3r:config_test`
- [ ] `bazel build --config=macos //src:PrusaSlicer`
- [ ] `bazel test --config=macos //tests/libslic3r:config_test`
- [ ] `rg -n "owned|bridge|defer|retire_when|status" tools/bazel/policies/proof_slice_bridges.md tools/bazel/policies/system_libraries.bzl tools/bazel/README.md`
- [ ] No task in this plan migrates GUI targets, packaging, CI completion, or broad third-party ownership outside the bounded slice
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Linux and macOS both prove the same bounded Bazel-owned CLI/core slice
- Remaining exceptions are explicit, narrow, and paired with retirement conditions
- Phase 3 closes with real cross-platform evidence instead of a single oversized proof attempt
  </success_criteria>

<output>
After completion, create `.planning/phases/03-migrate-core-targets-and-dependencies/03-04-SUMMARY.md`
</output>
