---
phase: 01-evaluate-build-system-fit
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .planning/phases/01-evaluate-build-system-fit/01-candidate-scorecard.md
  - .planning/phases/01-evaluate-build-system-fit/01-build-system-decision.md
  - .planning/phases/01-evaluate-build-system-fit/prototypes/bazel/MODULE.bazel
  - .planning/phases/01-evaluate-build-system-fit/prototypes/bazel/BUILD.bazel
  - .planning/phases/01-evaluate-build-system-fit/prototypes/meson/meson.build
  - .planning/phases/01-evaluate-build-system-fit/prototypes/README.md
  - .planning/PROJECT.md
autonomous: true
requirements:
  - EVAL-01
  - EVAL-02
user_setup:
  - Install or make available the candidate toolchains needed for evaluation (`bazelisk` or Bazel 9.x, Meson 1.10.x, Ninja, and the chosen compiler toolchain) if they are not already present in the environment.
must_haves:
  truths:
    - Bazel and Meson + Ninja are compared against the same weighted scorecard and hard gates.
    - The comparison uses a real PrusaSlicer app-oriented proof slice on Linux plus a macOS smoke-build check.
    - The final decision packet states one authoritative winner, explains why the loser lost, and makes Meson a valid fallback if Bazel fails the gates.
  artifacts:
    - .planning/phases/01-evaluate-build-system-fit/01-candidate-scorecard.md
    - .planning/phases/01-evaluate-build-system-fit/01-build-system-decision.md
    - .planning/phases/01-evaluate-build-system-fit/prototypes/README.md
  key_links:
    - The decision packet cites the scorecard results and the prototype proof artifacts for both candidates.
    - The chosen build-system outcome is recorded in .planning/PROJECT.md without reopening the Phase 1 evaluation criteria.
---

<objective>
Execute the Bazel vs Meson + Ninja comparison using the approved Phase 1 framework, capture the proof artifacts, and record the authoritative build-system decision.

Purpose: Finish Phase 1 with a decision packet strong enough that Phase 2 can start without re-litigating the build-system choice.
Output: Candidate proof artifacts, weighted scorecard results, a decision packet, and a PROJECT.md decision update.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-evaluate-build-system-fit/01-CONTEXT.md
@.planning/phases/01-evaluate-build-system-fit/01-RESEARCH.md
@.planning/phases/01-evaluate-build-system-fit/01-01-SUMMARY.md
@CMakeLists.txt
@src/CMakeLists.txt
@tests/CMakeLists.txt
@src/PrusaSlicer.cpp
@src/CLI/Run.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the candidate evaluation sandbox and onboarding notes</name>
  <files>.planning/phases/01-evaluate-build-system-fit/prototypes/bazel/MODULE.bazel, .planning/phases/01-evaluate-build-system-fit/prototypes/bazel/BUILD.bazel, .planning/phases/01-evaluate-build-system-fit/prototypes/meson/meson.build, .planning/phases/01-evaluate-build-system-fit/prototypes/README.md</files>
  <action>Create isolated Phase 1 prototype files for Bazel and Meson under the phase directory. The prototypes should reference the agreed representative PrusaSlicer slice rather than a toy target. Also write a short README describing the candidate commands, tool prerequisites, proof intent, and what each prototype does and does not cover. Keep the sandbox intentionally narrow: enough to evaluate candidate fitness, not enough to start full migration work.</action>
  <verify>test -f .planning/phases/01-evaluate-build-system-fit/prototypes/bazel/MODULE.bazel && test -f .planning/phases/01-evaluate-build-system-fit/prototypes/meson/meson.build && rg -n "Linux proof|macOS smoke-build|format|lint|clangd" .planning/phases/01-evaluate-build-system-fit/prototypes/README.md</verify>
  <done>Both candidate sandboxes and the shared README exist, reference the same proof slice, and keep the evaluation contained inside Phase 1.</done>
</task>

<task type="auto">
  <name>Task 2: Run the comparison and fill the weighted scorecard</name>
  <files>.planning/phases/01-evaluate-build-system-fit/01-candidate-scorecard.md</files>
  <action>Execute the comparison defined by Plan 01-01 using the candidate sandboxes and the agreed criteria. Capture Bazel and Meson results against the hard gates and weighted scorecard, including Linux app-target evidence, at least one Linux core test target, macOS smoke-build viability, editor metadata viability, format/lint command surface, CI command shape, maintenance burden, and bridge pressure. When evidence cannot be fully executed in the current environment, record that transparently and score the uncertainty rather than hand-waving it away.</action>
  <verify>rg -n "Hard-gate outcome|Weighted score|Linux proof|macOS smoke-build|Editor metadata|Bridge pressure" .planning/phases/01-evaluate-build-system-fit/01-candidate-scorecard.md</verify>
  <done>The scorecard compares both candidates on the same basis, shows hard-gate outcomes clearly, and is specific enough that another maintainer could audit the decision.</done>
</task>

<task type="auto">
  <name>Task 3: Write the decision packet and record the outcome</name>
  <files>.planning/phases/01-evaluate-build-system-fit/01-build-system-decision.md, .planning/PROJECT.md</files>
  <action>Write the final decision packet: selected candidate, rejection rationale for the loser, why the winner clears the gates, what remains deferred, and how Phase 2 should proceed from the result. Update PROJECT.md so the build-system choice moves from pending to an explicit project decision. If Bazel fails the agreed gates, record Meson + Ninja as the valid fallback outcome without treating the modernization effort as a failure.</action>
  <verify>rg -n "Selected candidate|Rejected candidate|Phase 2 implication|Deferred from Phase 1" .planning/phases/01-evaluate-build-system-fit/01-build-system-decision.md && rg -n "Bazel|Meson \\+ Ninja" .planning/PROJECT.md</verify>
  <done>The decision packet names one authoritative candidate, explains the loser, updates PROJECT.md, and leaves Phase 2 with a clear input instead of an open debate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] The candidate scorecard exists and shows both weighted scoring and hard-gate outcomes
- [ ] The decision packet cites the prototype artifacts and the scorecard rather than relying on unsupported narrative
- [ ] `PROJECT.md` records the build-system decision or fallback outcome explicitly
- [ ] The work stays inside Phase 1 scope and does not attempt packaging parity, Windows migration, or full dependency migration
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- One authoritative build-system outcome is recorded with rationale
- The comparison is auditable and reusable by later phases
  </success_criteria>

<output>
After completion, create `.planning/phases/01-evaluate-build-system-fit/01-02-SUMMARY.md`
</output>
