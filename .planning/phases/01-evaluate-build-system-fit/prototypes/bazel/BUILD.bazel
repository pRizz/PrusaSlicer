load("@hedron_compile_commands//:refresh_compile_commands.bzl", "refresh_compile_commands")
load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_test")

package(default_visibility = ["//visibility:public"])

cc_binary(
    name = "prusaslicer_cli_eval",
    srcs = [
        "src/PrusaSlicer.cpp",
        "src/CLI/Run.cpp",
        "src/CLI/Setup.cpp",
    ],
    copts = [
        "-DSLIC3R_GUI=0",
    ],
    includes = [
        "src",
    ],
    deps = [
        # Deliberately omitted in Phase 1 prototype to surface dependency
        # modeling and bridge pressure before any real migration work begins.
    ],
)

cc_test(
    name = "libslic3r_core_eval_test",
    srcs = [
        "tests/libslic3r/test_config.cpp",
    ],
    includes = [
        "src",
        "tests",
    ],
    deps = [
        # Deliberately omitted in the prototype for the same reason.
    ],
)

refresh_compile_commands(
    name = "refresh_compile_commands",
    targets = {
        "//:prusaslicer_cli_eval": "",
        "//:libslic3r_core_eval_test": "",
    },
)
