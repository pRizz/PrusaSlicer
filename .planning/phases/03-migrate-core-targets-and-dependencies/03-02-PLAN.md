---
phase: 03-migrate-core-targets-and-dependencies
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - MODULE.bazel
  - tools/bazel/deps/BUILD.bazel
  - tools/bazel/deps/proof_slice_deps.bzl
  - tools/bazel/policies/proof_slice_bridges.md
  - src/BUILD.bazel
  - src/CLI/BUILD.bazel
  - src/libslic3r/BUILD.bazel
autonomous: true
requirements:
  - BLD-02
  - DEPS-01
  - DEPS-03
user_setup: []
must_haves:
  truths:
    - macOS still builds the same `//src:PrusaSlicer` label, but a small Bazel-owned CLI/core source subset now sits behind it instead of only a binary shell.
    - The owned source subset is intentionally smaller than the broad `libslic3r` cut that previously ballooned and it stays bounded to one useful seam near the CLI path.
    - Third-party ownership is added only for the direct needs of that seam; GUI and large deferred subsystems remain outside the owned set.
  artifacts:
    - src/CLI/BUILD.bazel
    - src/libslic3r/BUILD.bazel
    - tools/bazel/deps/proof_slice_deps.bzl
    - tools/bazel/policies/proof_slice_bridges.md
  key_links:
    - `//src:PrusaSlicer` reuses the wave-1 binary label while replacing part of the initial handoff with a Bazel-owned CLI/core library target.
    - `tools/bazel/deps/proof_slice_deps.bzl` declares only the direct dependencies proven by the narrowed seam.
    - `tools/bazel/policies/proof_slice_bridges.md` shrinks or retires the wave-1 bridge instead of allowing it to grow.
---

<objective>
Deepen the macOS proof from a binary-shell handoff into one small Bazel-owned CLI/core seam with explicit generated inputs and a tightly bounded dependency set.

Purpose: Prove real source ownership in Phase 3 without repeating the earlier mistake of trying to absorb too much of `libslic3r` at once.
Output: A small owned core seam behind `//src:PrusaSlicer`, a proof-slice dependency manifest for that seam, and a reduced bridge surface.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-CONTEXT.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-RESEARCH.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-01-SUMMARY.md
@.planning/codebase/STACK.md
@.planning/codebase/STRUCTURE.md
@MODULE.bazel
@tools/bazel/deps/BUILD.bazel
@tools/bazel/policies/proof_slice_bridges.md
@src/PrusaSlicer.cpp
@src/CLI/Run.cpp
@src/CLI/Setup.cpp
@src/libslic3r/CMakeLists.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Choose one useful CLI/core seam and source-own only that seam</name>
  <files>src/BUILD.bazel, src/CLI/BUILD.bazel, src/libslic3r/BUILD.bazel</files>
  <action>Identify the smallest meaningful slice behind the CLI entry path that proves Bazel-owned core migration on macOS. Model it as dedicated `cc_library` targets and stop at the first seam that would otherwise drag in a large `libslic3r` subtree. The goal is not maximum source count; the goal is one defensible ownership boundary that is clearly smaller than the failed wave-1 attempt.</action>
  <verify>`bazel query 'deps(//src:PrusaSlicer)'`</verify>
  <done>The build graph shows a small owned CLI/core seam behind `//src:PrusaSlicer` and does not explode into a broad internal subtree.</done>
</task>

<task type="auto">
  <name>Task 2: Promote directly required generated inputs and shrink the temporary bridge</name>
  <files>src/BUILD.bazel, src/CLI/BUILD.bazel, src/libslic3r/BUILD.bazel, tools/bazel/policies/proof_slice_bridges.md</files>
  <action>Convert every directly required generated header or build product touched by the chosen seam into an explicit Bazel input. Then reduce the temporary handoff to only the parts that still cannot be source-owned in this wave. The bridge may remain only if its surface is now strictly smaller than the owned seam around it; if it expands, back out and choose a tighter seam.</action>
  <verify>`rg -n "generated|retire_when|status|scope" tools/bazel/policies/proof_slice_bridges.md src/BUILD.bazel src/CLI/BUILD.bazel src/libslic3r/BUILD.bazel`</verify>
  <done>The bridge surface is smaller than wave 1 and generated inputs are explicit instead of hidden in legacy build side effects.</done>
</task>

<task type="auto">
  <name>Task 3: Own only the third-party dependencies proven by the narrowed seam</name>
  <files>MODULE.bazel, tools/bazel/deps/BUILD.bazel, tools/bazel/deps/proof_slice_deps.bzl, tools/bazel/policies/proof_slice_bridges.md</files>
  <action>Add Bazel-owned dependency metadata only for the direct third-party libraries the seam actually compiles against. Keep the cut line strict: do not pre-own wxWidgets, OpenGL, OpenVDB, OCCT, `admesh`, or other broad legacy dependencies unless this seam demonstrably requires them, and if it does, re-scope the seam before accepting them.</action>
  <verify>`rg -n "Boost|Eigen|TBB|cereal|curl|EXPAT|PNG|wx|OpenVDB|OCCT|admesh" MODULE.bazel tools/bazel/deps/proof_slice_deps.bzl`</verify>
  <done>The proof-slice dependency manifest names only the direct inputs of the narrowed seam and excludes the large subsystems explicitly deferred from this wave.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bazel build --config=macos //src:PrusaSlicer`
- [ ] `bazel run --config=macos //src:PrusaSlicer -- --help`
- [ ] `rg -n "retire_when|status|scope" tools/bazel/policies/proof_slice_bridges.md`
- [ ] `rg -n "wx|OpenGL|OpenVDB|OCCT|admesh" tools/bazel/deps/proof_slice_deps.bzl` shows those broad deferred subsystems are still out unless the plan explicitly documents a re-scope
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- `//src:PrusaSlicer` now contains a real Bazel-owned CLI/core seam on macOS
- The owned seam is deliberately smaller than the earlier failed proof slice
- Dependency ownership stays bounded to what this seam proves directly
  </success_criteria>

<output>
After completion, create `.planning/phases/03-migrate-core-targets-and-dependencies/03-02-SUMMARY.md`
</output>
