---
phase: 03-migrate-core-targets-and-dependencies
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - tools/bazel/deps/proof_slice_deps.bzl
  - tools/bazel/policies/proof_slice_bridges.md
  - tests/BUILD.bazel
  - tests/libslic3r/BUILD.bazel
  - src/libslic3r/BUILD.bazel
autonomous: true
requirements:
  - BLD-02
  - DEPS-01
  - DEPS-03
user_setup: []
must_haves:
  truths:
    - `//tests/libslic3r:config_test` runs on macOS against the same Bazel-owned core seam used by `//src:PrusaSlicer`.
    - The chosen test remains intentionally narrow and does not collapse into a migration of the monolithic `libslic3r_tests` suite.
    - Any dependency or generated-file pressure revealed by the test is either explicitly owned or recorded as one narrow temporary bridge.
  artifacts:
    - tests/BUILD.bazel
    - tests/libslic3r/BUILD.bazel
    - tools/bazel/deps/proof_slice_deps.bzl
    - tools/bazel/policies/proof_slice_bridges.md
  key_links:
    - `//tests/libslic3r:config_test` depends on the same Bazel-owned CLI/core libraries already proven behind `//src:PrusaSlicer`.
    - The dependency manifest remains the shared source of truth for both the binary and the test slice.
    - The bridge inventory records only the exceptions still needed after real test execution, not hypothetical future bridges.
---

<objective>
Add `//tests/libslic3r:config_test` on macOS so the same bounded slice already migrated behind the `PrusaSlicer` binary is exercised by one real core test.

Purpose: Force the Phase 3 proof to survive one real test target without widening into the entire `tests/libslic3r` surface.
Output: The `config_test` Bazel target, updated dependency ownership for anything that test genuinely proves, and a bridge inventory refreshed from actual test pressure.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-CONTEXT.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-RESEARCH.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-02-SUMMARY.md
@.planning/codebase/TESTING.md
@tests/CMakeLists.txt
@tests/catch_main.hpp
@tests/libslic3r/CMakeLists.txt
@tests/libslic3r/test_config.cpp
@tests/libslic3r/libslic3r_tests.cpp
@src/libslic3r/BUILD.bazel
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `//tests/libslic3r:config_test` as the bounded Phase 3 test target</name>
  <files>tests/BUILD.bazel, tests/libslic3r/BUILD.bazel</files>
  <action>Build a dedicated Bazel target around `tests/libslic3r/test_config.cpp` and keep that target as the explicit Phase 3 representative test. Reject any approach that requires migrating the whole `libslic3r_tests` bundle or large fixture-heavy suites in this wave.</action>
  <verify>`bazel query //tests/libslic3r:all`</verify>
  <done>`//tests/libslic3r:config_test` exists and its scope is clearly smaller than the monolithic legacy test bundle.</done>
</task>

<task type="auto">
  <name>Task 2: Reuse the same owned core seam in `config_test`</name>
  <files>tests/libslic3r/BUILD.bazel, src/libslic3r/BUILD.bazel</files>
  <action>Wire `//tests/libslic3r:config_test` to the same Bazel-owned core libraries already sitting behind `//src:PrusaSlicer`. Add only the minimum Catch2 harness and shared test support needed to run that case. If the test reveals a deeper core dependency cut than the binary required, treat that as evidence for a small incremental ownership change, not permission to broaden the proof indiscriminately.</action>
  <verify>`bazel test --config=macos //tests/libslic3r:config_test`</verify>
  <done>`//tests/libslic3r:config_test` executes on macOS against the same owned seam as the binary, with no fallback to the monolithic legacy suite.</done>
</task>

<task type="auto">
  <name>Task 3: Refresh the bounded ownership and bridge inventory from test evidence</name>
  <files>tools/bazel/deps/proof_slice_deps.bzl, tools/bazel/policies/proof_slice_bridges.md, tests/libslic3r/BUILD.bazel</files>
  <action>Audit everything newly required by the representative test and force each item into one of two buckets in this wave: owned now because the binary-plus-test slice truly needs it, or temporarily bridged because the item is still a narrow blocker. Keep the inventory honest by removing speculative entries that the test never touched.</action>
  <verify>`rg -n "config_test|retire_when|status|owner" tools/bazel/policies/proof_slice_bridges.md tools/bazel/deps/proof_slice_deps.bzl tests/libslic3r/BUILD.bazel`</verify>
  <done>The proof-slice dependency set and bridge inventory now reflect actual binary-plus-test execution on macOS instead of assumptions.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bazel build --config=macos //src:PrusaSlicer`
- [ ] `bazel test --config=macos //tests/libslic3r:config_test`
- [ ] `rg -n "libslic3r_tests|monolithic|full suite" tests/libslic3r/BUILD.bazel` does not show a broad suite migration
- [ ] `rg -n "retire_when|owner|status" tools/bazel/policies/proof_slice_bridges.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- macOS proves `//tests/libslic3r:config_test` against the same bounded slice as the binary
- Test pressure updates ownership and bridge decisions without widening the slice uncontrolled
- The phase now has binary evidence and test evidence before Linux parity begins
  </success_criteria>

<output>
After completion, create `.planning/phases/03-migrate-core-targets-and-dependencies/03-03-SUMMARY.md`
</output>
