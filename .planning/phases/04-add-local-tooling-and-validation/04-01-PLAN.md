---
phase: 04-add-local-tooling-and-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prusa
  - .bazelrc
  - tools/bazel/BUILD.bazel
  - tools/bazel/README.md
  - tests/BUILD.bazel
  - tests/libslic3r/BUILD.bazel
  - tests/slic3rutils/BUILD.bazel
  - tests/fff_print/BUILD.bazel
  - README.md
autonomous: true
requirements:
  - BLD-04
user_setup: []
must_haves:
  truths:
    - Contributors can run one bounded authoritative Bazel test surface through `./prusa test` on both Linux and macOS without defaulting to `bazel test //...`.
    - The same named Bazel test labels are used on Linux and macOS, with any remaining platform gap called out explicitly as a tracked exception instead of being hidden behind the wrapper.
    - The Phase 4 test front door is honest about scope: it covers a non-GUI smoke surface plus at least one broader core regression-oriented target, not full CTest parity.
  artifacts:
    - prusa
    - .bazelrc
    - tools/bazel/BUILD.bazel
    - tools/bazel/README.md
    - tests/BUILD.bazel
    - tests/libslic3r/BUILD.bazel
    - README.md
  key_links:
    - `prusa` maps `test` to a bounded authoritative Bazel target set rather than `//...`.
    - `tools/bazel/BUILD.bazel` exposes one visible test-suite label that the wrapper and docs can point at directly.
    - Test BUILD files add only bounded non-GUI targets that reuse the migrated proof slice instead of reintroducing legacy CTest orchestration as the primary path.
---

<objective>
Create the Phase 4 local test front door: a bounded, contributor-usable Bazel test surface that runs on Linux and macOS through the same labels and the `./prusa test` wrapper entry point.

Purpose: Satisfy the phase's credibility requirement for local testing without overstating coverage or reopening full-suite migration work.
Output: One explicit Bazel-owned test suite label, wrapper/docs updates for that bounded surface, and cross-platform verification commands for the same smoke-plus-regression test set.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/skills/gsd-execute-phase/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-add-local-tooling-and-validation/04-CONTEXT.md
@.planning/phases/04-add-local-tooling-and-validation/04-RESEARCH.md
@.planning/phases/03-migrate-core-targets-and-dependencies/03-04-SUMMARY.md
@prusa
@.bazelrc
@tools/bazel/BUILD.bazel
@tools/bazel/README.md
@tests/BUILD.bazel
@tests/CMakeLists.txt
@tests/libslic3r/BUILD.bazel
@tests/libslic3r/CMakeLists.txt
@tests/slic3rutils/CMakeLists.txt
@tests/fff_print/CMakeLists.txt
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the bounded authoritative Bazel test surface</name>
  <files>tools/bazel/BUILD.bazel, tests/BUILD.bazel, tests/libslic3r/BUILD.bazel, tests/slic3rutils/BUILD.bazel, tests/fff_print/BUILD.bazel</files>
  <action>Create one obvious Bazel-owned test-suite label under `tools/bazel/` for the Phase 4 local test surface. Anchor it on `//tests/libslic3r:config_test` and add only the smallest additional non-GUI regression-oriented target or targets that stay cross-platform and depend on the already migrated core slice. Prefer existing test directories that remain CLI/core adjacent. Reject any approach that pulls in GUI, packaging, or the full legacy `tests/` tree just to claim broader coverage.</action>
  <verify>`bazel query //tools/bazel:test_suite && bazel query 'tests(//tools/bazel:test_suite)'`</verify>
  <done>The repo has one visible authoritative Bazel test-suite label for Phase 4, and that label resolves to a bounded non-GUI core surface rather than to `//...` or a hidden ad hoc target list.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint `./prusa test` at the bounded cross-platform suite</name>
  <files>prusa, .bazelrc, tools/bazel/BUILD.bazel, tools/bazel/README.md</files>
  <action>Change the wrapper so `./prusa test --platform macos` and `./prusa test --platform linux` run the same named authoritative Bazel suite instead of `bazel test --config=dev --config=<platform> //...`. Keep the wrapper thin and explicit: the help text and dry-run output must show the exact bounded Bazel label, the same config names on both platforms, and any tracked exception path that still requires legacy CTest outside this bounded suite. Do not make CTest the default fallback.</action>
  <verify>`./prusa test --platform macos --dry-run && ./prusa test --platform linux --dry-run`</verify>
  <done>Contributors can discover and run the same bounded authoritative test surface from the root wrapper on both supported platforms, and the wrapper output makes the actual Bazel target set explicit.</done>
</task>

<task type="auto">
  <name>Task 3: Publish honest test-scope guidance and platform verification</name>
  <files>README.md, tools/bazel/README.md, prusa</files>
  <action>Document the new test front door so contributors understand exactly what Phase 4 covers. Add or update concise guidance that names the authoritative `./prusa test` path, the underlying Bazel suite label, the smoke target (`//tests/libslic3r:config_test`), the additional regression-oriented target, and any explicit exclusions or tracked exceptions that still stay on legacy CTest. Make the wording clear that this is the credible local core test surface for now, not full test-suite parity.</action>
  <verify>`rg -n "prusa test|config_test|CTest|exception|bounded|Phase 4" README.md tools/bazel/README.md prusa`</verify>
  <done>The docs and wrapper help describe one honest local test workflow, its precise scope, and the temporary gaps that remain outside the authoritative Bazel path.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bazel test --config=dev --config=macos //tools/bazel:test_suite`
- [ ] `bazel test --config=dev --config=linux //tools/bazel:test_suite`
- [ ] `./prusa test --platform macos`
- [ ] `./prusa test --platform linux`
- [ ] `bazel query 'tests(//tools/bazel:test_suite)'` shows `//tests/libslic3r:config_test` plus at least one broader non-GUI regression-oriented target
- [ ] `rg -n "ctest|legacy exception|bounded|config_test|test_suite" README.md tools/bazel/README.md prusa` confirms the scope boundary is explicit
- [ ] No task in this plan expands into CI wiring, GUI test migration, or full CTest parity
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Contributors can run the same bounded authoritative Bazel test surface on Linux and macOS
- The root test front door is honest about scope and does not pretend full legacy-suite parity
- Remaining legacy-only exceptions are visible and documented instead of hidden behind the wrapper
  </success_criteria>

<output>
After completion, create `.planning/phases/04-add-local-tooling-and-validation/04-01-SUMMARY.md`
</output>
